# 컨테이너 실행용 환경변수 (.env)
# 이 파일은 docker-compose.yml 에서 참조됩니다.

# 포트 매핑 (호스트:컨테이너)
# HTTP는 컨테이너 9000으로, WS는 8765로 고정되어 있습니다.
HTTP_PORT=9000
WS_PORT=8765

# (선택) 리눅스에서 파일 권한 매핑을 위해 호스트 UID/GID 지정
# 필요 시 `id -u`, `id -g` 값으로 변경하세요.
UID=1000
GID=1000

# 간단 로그인 비밀번호 (선택)
# 값을 지정하면 접속 시 로그인 모달이 표시됩니다.
# 비워두면 인증 없이 사용됩니다.
APP_LOGIN_PASSWORD=hu23bcCHAT!

# (선택) Factory.ai API 키 — Droid CLI를 키 기반으로 사용할 때만 필요
# OAuth로 인증된 `~/.factory`를 볼륨 마운트한다면 비워두어도 됩니다.
FACTORY_API_KEY=60e6ed64a2544565916ef223f875807b.maD2YtxxGeLjiyx5
